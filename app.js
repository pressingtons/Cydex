const DAILY_XP_CAP = 250;
const DEFAULT_STATE = { xp: 1240, totalXp: 1240, daily: { date: '', earned: 0, claims: {} }, objectiveRewards: {}, labRewards: {}, purchasedItems: [], readiness: 42, recall: 12, lessons: 3, answers: 0, correct: 0, streak: 7, soundOn: true, goal: 'CompTIA Security+', activeCertification: 'security-plus', currentCertFilter: 'all', lastStudy: null };
const storageKey = 'cydex-state-v3';
let state = normalizeState({ ...DEFAULT_STATE, ...safeJson(localStorage.getItem(storageKey) || localStorage.getItem('cydex-state-v2')) });
let currentUser = safeJson(localStorage.getItem('cydex-user'));
let questionIndex = 0;
let authMode = 'signup';

const certifications = [
  { id: 'tech-plus', provider: 'CompTIA', title: 'Tech+', code: 'FC0', level: 'Foundation', area: 'IT foundations', description: 'A gentle on-ramp to computing, networking, security, and data basics.' },
  { id: 'a-plus', provider: 'CompTIA', title: 'A+', code: 'Core 1 + Core 2', level: 'Core', area: 'IT support', description: 'Hardware, operating systems, troubleshooting, and customer support.' },
  { id: 'network-plus', provider: 'CompTIA', title: 'Network+', code: 'N10', level: 'Core', area: 'Networking', description: 'Network design, operations, troubleshooting, and security basics.' },
  { id: 'security-plus', provider: 'CompTIA', title: 'Security+', code: 'SY0', level: 'Core', area: 'Cybersecurity', description: 'The essential baseline for risk, threats, architecture, and operations.' },
  { id: 'linux-plus', provider: 'CompTIA', title: 'Linux+', code: 'XK0', level: 'Professional', area: 'Linux', description: 'Linux configuration, security, scripting, and operations.' },
  { id: 'cloud-plus', provider: 'CompTIA', title: 'Cloud+', code: 'CV0', level: 'Professional', area: 'Cloud', description: 'Cloud architecture, deployment, security, and troubleshooting.' },
  { id: 'data-plus', provider: 'CompTIA', title: 'Data+', code: 'DA0', level: 'Professional', area: 'Data', description: 'Data concepts, analytics, governance, and visualization.' },
  { id: 'project-plus', provider: 'CompTIA', title: 'Project+', code: 'PK0', level: 'Professional', area: 'Project management', description: 'Technology project lifecycles, communication, and risk.' },
  { id: 'cysa-plus', provider: 'CompTIA', title: 'CySA+', code: 'CS0', level: 'Professional', area: 'Defensive security', description: 'Detection, incident response, vulnerability management, and reporting.' },
  { id: 'pentest-plus', provider: 'CompTIA', title: 'PenTest+', code: 'PT0', level: 'Professional', area: 'Offensive security', description: 'Ethical penetration testing planning, execution, and reporting.' },
  { id: 'securityx', provider: 'CompTIA', title: 'SecurityX', code: 'CAS', level: 'Advanced', area: 'Security architecture', description: 'Advanced architecture, engineering, governance, and risk.' },
  { id: 'ccst-networking', provider: 'Cisco', title: 'CCST Networking', code: 'Entry', level: 'Foundation', area: 'Networking', description: 'A first technical networking credential and bridge toward CCNA.' },
  { id: 'ccst-cyber', provider: 'Cisco', title: 'CCST Cybersecurity', code: 'Entry', level: 'Foundation', area: 'Cybersecurity', description: 'Security concepts and junior analyst fundamentals.' },
  { id: 'ccna', provider: 'Cisco', title: 'CCNA', code: '200-301', level: 'Associate', area: 'Networking', description: 'Network fundamentals, access, IP connectivity, security, and automation.' },
  { id: 'cyberops', provider: 'Cisco', title: 'Cybersecurity Associate', code: '200-201', level: 'Associate', area: 'Security operations', description: 'Monitoring, intrusion analysis, and operational security fundamentals.' },
  { id: 'devnet', provider: 'Cisco', title: 'DevNet Associate', code: '200-901', level: 'Associate', area: 'Automation', description: 'APIs, application development, infrastructure, and automation.' }
];
const rewards = [
  { id: 'pbq-sandbox', name: 'Security+ PBQ Sandbox', cost: 650, type: 'Cydex Mastery lab', description: 'Unlock an original mini-lab pack for networks, access control, and incident response.' },
  { id: 'ccna-cli', name: 'CCNA CLI Warm-up', cost: 700, type: 'Cydex Mastery lab', description: 'Unlock original router-command drills and guided configuration checks.' },
  { id: 'soc-drills', name: 'SOC Triage Drills', cost: 800, type: 'Cydex Mastery lab', description: 'Unlock original alert-triage cases for the CySA+ and Cybersecurity Associate paths.' },
  { id: 'focus-sprint', name: 'Focus Sprint Pack', cost: 250, type: 'Study tool', description: 'Unlock a 7-day mission board and momentum badge set.' },
  { id: 'partner-certmaster', name: 'Official CertMaster access', cost: null, type: 'Partner-only', description: 'Not purchasable with XP. This needs an official CompTIA partnership and separate provider access.' }
];
const objectives = [
  { domain: 'General Security Concepts', code: '1.0', objectives: [['1.1', 'Security controls', 'done'], ['1.2', 'Cryptographic concepts', 'current'], ['1.3', 'Authentication factors', ''], ['1.4', 'Security principles', '']] },
  { domain: 'Threats, Vulnerabilities & Mitigations', code: '2.0', objectives: [['2.1', 'Threat actors', 'done'], ['2.2', 'Attack vectors', 'done'], ['2.3', 'Vulnerability management', ''], ['2.4', 'Malware', '']] },
  { domain: 'Security Architecture', code: '3.0', objectives: [['3.1', 'Enterprise security', ''], ['3.2', 'Cloud security', ''], ['3.3', 'Resilience', ''], ['3.4', 'Zero trust', '']] }
];
const focusAreas = [['WPA3 and wireless encryption', 31], ['Public key infrastructure', 38], ['Identity federation', 44]];
const questions = [
  { id: 'security-crypto-bulk', tag: 'OBJ 1.2', prompt: 'A company needs to encrypt large database backups quickly. Which approach is the best fit?', answers: ['Asymmetric encryption with a public key', 'Symmetric encryption with a shared secret key', 'Hashing each backup with SHA-256', 'A digital signature for every file'], correct: 1, explanation: 'Symmetric encryption uses one shared key and is much faster than asymmetric encryption for bulk data. Hashing verifies integrity, while signatures verify authenticity; neither encrypts the backup.' },
  { id: 'security-integrity', tag: 'OBJ 1.2', prompt: 'Which cryptographic property confirms that a message has not been altered in transit?', answers: ['Confidentiality', 'Integrity', 'Availability', 'Non-repudiation'], correct: 1, explanation: 'Integrity means data remains accurate and unchanged. A hash or message authentication code can help detect modification.' },
  { id: 'security-phishing', tag: 'OBJ 2.2', prompt: 'An attacker sends a carefully crafted email that appears to come from payroll. What is this attack vector?', answers: ['Phishing', 'Shoulder surfing', 'Evil twin', 'Privilege escalation'], correct: 0, explanation: 'Phishing uses deceptive messages to persuade people to disclose information or take unsafe actions.' }
];

function safeJson(value) { try { return value ? JSON.parse(value) : {}; } catch { return {}; } }
function today() { return new Date().toISOString().slice(0, 10); }
function normalizeState(raw) { const next = { ...DEFAULT_STATE, ...raw }; next.totalXp = Number.isFinite(raw.totalXp) ? raw.totalXp : next.xp; next.daily = { date: '', earned: 0, claims: {}, ...(raw.daily || {}) }; next.objectiveRewards = raw.objectiveRewards || {}; next.labRewards = raw.labRewards || {}; next.purchasedItems = raw.purchasedItems || []; if (next.daily.date !== today()) next.daily = { date: today(), earned: 0, claims: {} }; return next; }
function byId(id) { return document.getElementById(id); }
function persist() { state = normalizeState(state); localStorage.setItem(storageKey, JSON.stringify(state)); }
function displayName() { return currentUser?.name || 'Alex Parker'; }
function firstName() { return displayName().split(' ')[0]; }
function initials() { return displayName().split(' ').map(word => word[0]).slice(0, 2).join('').toUpperCase(); }
function level() { return Math.floor(state.totalXp / 350) + 1; }
function xpToNext() { return 350 - (state.totalXp % 350); }
function activeCert() { return certifications.find(cert => cert.id === state.activeCertification) || certifications.find(cert => cert.id === 'security-plus'); }
function toast(message, tone = 'success') { const el = byId('toast'); el.textContent = message; el.dataset.tone = tone; el.classList.add('show'); clearTimeout(window.toastTimer); window.toastTimer = setTimeout(() => el.classList.remove('show'), 3800); }
function playTone(kind) { if (!state.soundOn || !window.AudioContext) return; const context = new AudioContext(); const oscillator = context.createOscillator(); const gain = context.createGain(); oscillator.frequency.value = kind === 'good' ? 560 : 180; oscillator.type = kind === 'good' ? 'sine' : 'triangle'; gain.gain.setValueAtTime(.055, context.currentTime); gain.gain.exponentialRampToValueAtTime(.001, context.currentTime + .14); oscillator.connect(gain); gain.connect(context.destination); oscillator.start(); oscillator.stop(context.currentTime + .14); }
function applyXp(delta, reason, updateLifetime = true) { const before = state.xp; state.xp = Math.max(0, state.xp + delta); if (updateLifetime) state.totalXp = Math.max(0, state.totalXp + delta); persist(); renderState(); const actual = state.xp - before; if (actual) toast(`${actual > 0 ? '+' : ''}${actual} XP - ${reason}`, actual > 0 ? 'success' : 'warning'); }
function syncAccountClaim(key) { const token = localStorage.getItem('cydex-session'); if (!currentUser || !token) return; const separator = key.indexOf('-'); const type = key.slice(0, separator); const claimKey = key.slice(separator + 1); request('/api/progress/claim', { token, type, key: claimKey }).then(result => { if (result.progress) { state = normalizeState({ ...state, ...result.progress }); persist(); renderState(); } if (!result.claimed) toast(result.message || 'This account reward was already claimed.', 'warning'); }).catch(() => toast('Account progress could not sync. Your local safeguards are still active.', 'warning')); }
function claimLocalReward(key, amount, reason) { persist(); const serverKey = key.replace('-', ':'); if (state.daily.claims[key] || state.daily.claims[serverKey]) { toast('Nice work - that objective has already earned XP today. Keep practicing for mastery.', 'warning'); return false; } const available = Math.max(0, DAILY_XP_CAP - state.daily.earned); if (!available) { toast(`Daily XP cap reached (${DAILY_XP_CAP}). Practice still improves your readiness.`, 'warning'); return false; } const earned = Math.min(amount, available); state.daily.claims[key] = today(); state.daily.claims[serverKey] = today(); state.daily.earned += earned; applyXp(earned, reason); syncAccountClaim(key); return true; }
function applyPenaltyOnce(key, reason) { persist(); const penaltyKey = `penalty-${key}`; if (state.daily.claims[penaltyKey]) return; state.daily.claims[penaltyKey] = today(); applyXp(-5, reason); }
function renderState() {
  persist(); byId('streakCount').textContent = state.streak; byId('xpValue').textContent = state.xp.toLocaleString(); byId('vaultXpValue').textContent = `${state.xp.toLocaleString()} XP`; byId('levelValue').textContent = `LVL ${level()}`; byId('levelCaption').textContent = `${xpToNext()} XP to level ${level() + 1}`;
  byId('dailyXpValue').textContent = `${state.daily.earned} / ${DAILY_XP_CAP}`; byId('dailyXpBar').style.width = `${Math.min(100, state.daily.earned / DAILY_XP_CAP * 100)}%`; byId('vaultCapText').textContent = `${state.daily.earned} of ${DAILY_XP_CAP} daily XP earned.`;
  byId('readinessValue').textContent = `${state.readiness}%`; byId('readinessBar').style.width = `${state.readiness}%`; byId('reviewCount').textContent = state.recall; byId('recallRemaining').textContent = state.recall; byId('terminalScore').textContent = `${state.correct} / ${state.answers}`;
  byId('completedObjectives').textContent = `${state.lessons} / 18`; byId('pathProgress').style.width = `${Math.round((state.lessons / 18) * 100)}%`; byId('unlockProgress').textContent = `${state.readiness}% / 60%`;
  byId('profileName').textContent = displayName(); byId('heroName').textContent = firstName(); byId('avatarInitials').textContent = initials(); byId('profileRole').textContent = `${activeCert().title} Candidate`; byId('soundButton').textContent = `Sound: ${state.soundOn ? 'on' : 'off'}`;
  byId('soundInput').checked = state.soundOn; byId('profileNameInput').value = displayName(); byId('goalInput').value = state.goal; byId('accountButton').textContent = currentUser ? 'Account' : 'Sign up / Log in'; byId('streakMessage').textContent = state.lastStudy === today() ? 'Mission complete. Momentum secured.' : 'Study today to protect it.';
}
function renderDashboard() {
  byId('skillTree').innerHTML = [['1.1', 'Controls', 'done'], ['1.2', 'Crypto', 'current'], ['1.3', 'Auth', 'locked'], ['1.4', 'Principles', 'locked'], ['2.1', 'Threats', 'locked']].map(([code, name, status]) => `<button class="tree-node ${status}" ${status === 'current' ? 'data-action="start-mission"' : ''} ${status === 'locked' ? 'disabled' : ''}><small>${code} / ${status === 'done' ? 'mastered' : status === 'current' ? 'in progress' : 'locked'}</small><strong>${name}${status === 'done' ? ' +' : ''}</strong></button>`).join('');
  byId('focusList').innerHTML = focusAreas.map(([label, confidence]) => `<article class="focus-item"><div><span>OBJECTIVE</span><span>${confidence}% confident</span></div><strong>${label}</strong><div class="progress"><i style="width:${confidence}%"></i></div></article>`).join('');
  byId('objectiveMap').innerHTML = objectives.map(({ domain, code, objectives: items }) => `<section class="domain"><div class="domain-head"><h2>${domain}</h2><span>DOMAIN ${code}</span></div><div class="objective-items">${items.map(([number, label, status]) => `<button class="objective ${status}" ${status === 'current' ? 'data-action="start-mission"' : ''} ${status === '' ? 'disabled' : ''}><i></i><span>${number} - ${label}${status === 'done' ? ' +' : ''}</span></button>`).join('')}</div></section>`).join('');
}
function renderGoalOptions() { byId('goalInput').innerHTML = certifications.map(cert => `<option value="${cert.provider} ${cert.title}">${cert.provider} ${cert.title}</option>`).join(''); if (![...byId('goalInput').options].some(option => option.value === state.goal)) state.goal = `${activeCert().provider} ${activeCert().title}`; byId('goalInput').value = state.goal; }
function renderCertCatalog() { const filter = state.currentCertFilter; const selected = certifications.filter(cert => filter === 'all' || cert.provider === filter); byId('certCatalog').innerHTML = selected.map(cert => `<article class="cert-card ${cert.id === state.activeCertification ? 'active-cert' : ''}"><div class="cert-card-top"><span>${cert.provider}</span><small>${cert.level}</small></div><h2>${cert.title}</h2><p class="cert-code">${cert.code} / ${cert.area}</p><p>${cert.description}</p><button class="${cert.id === state.activeCertification ? 'outline-button' : 'primary-button'}" data-action="select-cert" data-cert="${cert.id}">${cert.id === state.activeCertification ? 'Active campaign' : 'Set as campaign'} <span>&rarr;</span></button></article>`).join(''); }
function renderRewards() { byId('rewardCatalog').innerHTML = rewards.map(reward => { const purchased = state.purchasedItems.includes(reward.id); const canBuy = reward.cost !== null && state.xp >= reward.cost && !purchased; const button = reward.cost === null ? `<button class="outline-button" disabled>Partner access required</button>` : purchased ? `<button class="outline-button" data-action="activate-reward" data-reward="${reward.id}">Unlocked - activate</button>` : `<button class="${canBuy ? 'primary-button' : 'outline-button'}" data-action="buy-reward" data-reward="${reward.id}" ${canBuy ? '' : 'disabled'}>${reward.cost} XP <span>&rarr;</span></button>`; return `<article class="reward-card ${purchased ? 'owned' : ''}"><div><span class="reward-type">${reward.type}</span><span class="reward-cost">${reward.cost === null ? 'Partner' : `${reward.cost} XP`}</span></div><h2>${reward.name}</h2><p>${reward.description}</p>${button}</article>`; }).join(''); }
function showView(view) { document.querySelectorAll('.view').forEach(el => el.classList.toggle('active', el.id === view)); document.querySelectorAll('.nav-link').forEach(el => el.classList.toggle('active', el.dataset.view === view)); document.querySelector('.sidebar').classList.remove('open'); if (view === 'practice') renderQuestion(); if (view === 'certifications') renderCertCatalog(); if (view === 'vault') renderRewards(); window.scrollTo({ top: 0, behavior: 'smooth' }); }
function renderQuestion() { const q = questions[questionIndex % questions.length]; byId('questionTag').textContent = q.tag; byId('questionWrap').innerHTML = `<div class="question-number">QUESTION ${questionIndex + 1} / ADAPTIVE DRILL</div><h2>${q.prompt}</h2><div class="answers">${q.answers.map((answer, index) => `<button class="answer" data-action="answer-question" data-answer="${index}"><span class="answer-key">${String.fromCharCode(65 + index)}</span><span>${answer}</span></button>`).join('')}</div>`; }
function answerQuestion(answer) { const q = questions[questionIndex % questions.length]; const buttons = document.querySelectorAll('[data-action="answer-question"]'); if (!buttons.length || buttons[0].disabled) return; buttons.forEach((button, index) => { button.disabled = true; if (index === q.correct) button.classList.add('correct'); if (index === answer && answer !== q.correct) button.classList.add('incorrect'); }); const correct = answer === q.correct; state.answers += 1; let earned = false; if (correct) { state.correct += 1; earned = claimLocalReward(`question-${q.id}`, 20, 'correct new question'); if (earned) state.readiness = Math.min(99, state.readiness + 1); playTone('good'); } else { state.recall += 1; state.readiness = Math.max(0, state.readiness - 1); applyPenaltyOnce(q.id, 'missed answer'); playTone('bad'); } persist(); renderState(); const feedback = document.createElement('div'); feedback.className = 'explanation'; feedback.innerHTML = `<strong>${correct ? earned ? 'CORRECT - +20 XP' : 'CORRECT - REINFORCEMENT, NO XP' : 'NOT QUITE - 5 XP, ADDED TO RECALL'}</strong><p>${q.explanation}</p><button class="primary-button next-question" data-action="next-question">Next question <span>&rarr;</span></button>`; byId('questionWrap').append(feedback); }
function completeLesson() { const key = `objective-${state.activeCertification}-1.2`; if (state.objectiveRewards[key]) { closeModal('lessonModal'); toast('This objective is already mastered. Review it anytime, but XP is awarded once.', 'warning'); return; } if (claimLocalReward(key, 80, 'mission complete')) { state.objectiveRewards[key] = today(); state.lessons = Math.min(18, state.lessons + 1); state.readiness = Math.min(99, state.readiness + 2); state.lastStudy = today(); persist(); renderDashboard(); } closeModal('lessonModal'); playTone('good'); }
function validateLab(form) { const fields = Object.fromEntries(new FormData(form)); const feedback = byId('labFeedback'); if (fields.firewall === 'deny' && fields.admin === 'mfa' && fields.vpn === 'on') { const key = 'lab-gateway-baseline'; if (state.labRewards[key]) { feedback.textContent = 'Configuration validated. You already earned this lab reward; keep experimenting.'; return; } state.labRewards[key] = today(); persist(); const earned = claimLocalReward(key, 50, 'mastery lab complete'); state.readiness = Math.min(99, state.readiness + (earned ? 2 : 0)); renderState(); feedback.textContent = earned ? 'Configuration validated. You denied unsolicited traffic, protected admins with MFA, and retained VPN access.' : 'Configuration validated. Daily XP is capped, but your lab mastery was recorded.'; playTone('good'); } else { feedback.textContent = 'Not secure yet. Deny unsolicited inbound traffic, require MFA for admins, and preserve approved VPN access.'; feedback.className = 'form-message bad-feedback'; playTone('bad'); } }
function buyReward(id) { const reward = rewards.find(item => item.id === id); if (!reward || reward.cost === null || state.purchasedItems.includes(id)) return; if (state.xp < reward.cost) { toast(`You need ${reward.cost - state.xp} more XP for this unlock.`, 'warning'); return; } state.xp -= reward.cost; state.purchasedItems.push(id); persist(); renderState(); renderRewards(); toast(`${reward.name} unlocked. It is now in your Cydex toolkit.`); playTone('good'); }
function selectCert(id) { const cert = certifications.find(item => item.id === id); if (!cert) return; state.activeCertification = cert.id; state.goal = `${cert.provider} ${cert.title}`; if (currentUser) { currentUser.goal = state.goal; localStorage.setItem('cydex-user', JSON.stringify(currentUser)); } persist(); renderGoalOptions(); renderState(); renderCertCatalog(); toast(`${cert.provider} ${cert.title} is now your active campaign.`); }
function openModal(id) { byId(id).hidden = false; document.body.classList.add('modal-open'); if (id === 'authModal') switchAuth(authMode); }
function closeModal(id) { byId(id).hidden = true; if (![...document.querySelectorAll('.modal-backdrop')].some(el => !el.hidden)) document.body.classList.remove('modal-open'); }
function closeAllModals() { document.querySelectorAll('.modal-backdrop').forEach(el => { el.hidden = true; }); document.body.classList.remove('modal-open'); }
function switchAuth(mode) { authMode = mode; byId('signupForm').hidden = mode !== 'signup'; byId('loginForm').hidden = mode !== 'login'; document.querySelectorAll('.auth-tab').forEach(tab => tab.classList.toggle('active', tab.dataset.authMode === mode)); byId('authMessage').textContent = ''; }
async function request(path, data) { const response = await fetch(path, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || 'Something went wrong. Please try again.'); return result; }
function finishAuth(result) { currentUser = result.user; localStorage.setItem('cydex-user', JSON.stringify(currentUser)); localStorage.setItem('cydex-session', result.sessionToken); state = normalizeState({ ...state, ...(currentUser.progress || {}), goal: currentUser.goal || state.goal }); persist(); renderGoalOptions(); renderState(); closeModal('authModal'); toast(result.emailStatus?.delivery === 'sent' ? 'Welcome email sent. Your Cydex ID is live.' : result.emailStatus?.delivery === 'existing' ? 'Welcome back. Your Cydex ID is ready.' : 'Account created. Welcome email queued for local delivery.'); }
async function submitSignup(form) { const message = byId('authMessage'); message.textContent = 'Creating your Cydex ID...'; try { finishAuth(await request('/api/auth/signup', Object.fromEntries(new FormData(form)))); form.reset(); } catch (error) { message.textContent = error.message; } }
async function submitLogin(form) { const message = byId('authMessage'); message.textContent = 'Checking your credentials...'; try { finishAuth(await request('/api/auth/login', Object.fromEntries(new FormData(form)))); form.reset(); } catch (error) { message.textContent = error.message; } }
function updateProfile(form) { const values = Object.fromEntries(new FormData(form)); const name = values.name.trim(); if (!name) return; currentUser = { ...(currentUser || {}), name, goal: values.goal }; localStorage.setItem('cydex-user', JSON.stringify(currentUser)); state.goal = values.goal; const matching = certifications.find(cert => `${cert.provider} ${cert.title}` === values.goal); if (matching) state.activeCertification = matching.id; state.soundOn = values.sound === 'on'; persist(); renderState(); renderCertCatalog(); closeModal('profileModal'); toast('Profile saved. Your learning space is yours.'); }

document.addEventListener('click', event => { const target = event.target.closest('[data-action]'); if (!target) return; const action = target.dataset.action; if (action === 'show-view') showView(target.dataset.view); if (action === 'start-mission') { closeAllModals(); openModal('lessonModal'); } if (action === 'close-modal') closeModal(target.dataset.modal); if (action === 'lesson-answer') { if (target.dataset.correct === 'true') { byId('lessonFeedback').className = 'check-feedback'; byId('lessonFeedback').textContent = 'Correct. Symmetric ciphers are the efficient choice for large data volumes.'; setTimeout(completeLesson, 650); } else { byId('lessonFeedback').className = 'check-feedback bad-feedback'; byId('lessonFeedback').textContent = 'Try again. Think about the option that avoids public/private-key overhead.'; playTone('bad'); } } if (action === 'answer-question') answerQuestion(Number(target.dataset.answer)); if (action === 'next-question') { questionIndex += 1; renderQuestion(); } if (action === 'practice-weak') showView('practice'); if (action === 'start-recall') { showView('practice'); toast('Recall mode enabled - weak objectives prioritized.'); } if (action === 'start-exam') { showView('practice'); toast('Readiness assessment started. Focus mode is on.'); } if (action === 'locked-exam') toast(`Build another ${Math.max(0, 60 - state.readiness)}% readiness to unlock this exam.`, 'warning'); if (action === 'toggle-menu') document.querySelector('.sidebar').classList.toggle('open'); if (action === 'toggle-sound') { state.soundOn = !state.soundOn; persist(); renderState(); toast(`Practice sounds ${state.soundOn ? 'enabled' : 'muted'}.`); if (state.soundOn) playTone('good'); } if (action === 'open-auth') currentUser ? openModal('profileModal') : openModal('authModal'); if (action === 'open-profile') openModal('profileModal'); if (action === 'open-plans') openModal('plansModal'); if (action === 'open-notifications') openModal('notificationsModal'); if (action === 'open-help') window.location.href = 'mailto:hello@cydex.dev?subject=Cydex%20early%20access'; if (action === 'switch-auth') switchAuth(target.dataset.authMode); if (action === 'filter-certs') { state.currentCertFilter = target.dataset.filter; persist(); document.querySelectorAll('.filter-button').forEach(button => button.classList.toggle('active', button === target)); renderCertCatalog(); } if (action === 'select-cert') selectCert(target.dataset.cert); if (action === 'buy-reward') buyReward(target.dataset.reward); if (action === 'activate-reward') { showView('mastery'); toast('Your Cydex Mastery content is ready. Select a lab scenario to begin.'); } if (action === 'logout') { currentUser = null; localStorage.removeItem('cydex-user'); localStorage.removeItem('cydex-session'); renderState(); closeModal('profileModal'); toast('Logged out of this device. Your local study data stays here.'); } if (action === 'reset-progress') { state = normalizeState({ ...DEFAULT_STATE, soundOn: state.soundOn, goal: state.goal, activeCertification: state.activeCertification }); persist(); renderState(); renderDashboard(); renderRewards(); toast('Study progress reset. Your account is unchanged.', 'warning'); } });
document.addEventListener('click', event => { if (event.target.classList.contains('modal-backdrop')) closeModal(event.target.id); }); document.addEventListener('keydown', event => { if (event.key === 'Escape') closeAllModals(); });
byId('signupForm').addEventListener('submit', event => { event.preventDefault(); submitSignup(event.currentTarget); }); byId('loginForm').addEventListener('submit', event => { event.preventDefault(); submitLogin(event.currentTarget); }); byId('profileForm').addEventListener('submit', event => { event.preventDefault(); updateProfile(event.currentTarget); }); byId('labForm').addEventListener('submit', event => { event.preventDefault(); validateLab(event.currentTarget); });

renderGoalOptions(); renderState(); renderDashboard(); renderCertCatalog(); renderRewards();
